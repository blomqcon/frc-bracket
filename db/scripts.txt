db.accounts.insert({email: "connor.blomquists@gmail.com", name: "Connor Blomquist", team: "2522", predictions: []});

db.createUser(
    {
      user: "admin",
      pwd: "password",
      roles: [
         { role: "dbAdmin", db: "frc-bracket" }
      ]
    }
)

/etc/mongo.conf
dbpath = /var/lib/mongodb
logfile = /var/log/mongodb.log

Start mongodb as dameon:
mongod --fork --logpath /var/log/mongodb.log

Start mongodb with access control: 
mongod --auth --port 27017 --dbpath /var/lib/mongodb

Shutdown Server
mongod --dbpath /var/lib/mongodb --shutdown